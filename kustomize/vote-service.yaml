apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote-nodeport-svc
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    port: 80
    targetPort: 80
  selector:
    app: vote
  
